You are an expert information extraction agent for scientific articles.
Your task is to extract the paper's core hypothesis statement(s) that correspond to a provided Abstract Finding.

Definition (Hypothesis statement)\\
A hypothesis statement is one or more complete sentences in the main text that explicitly state a testable expectation, prediction, or proposed relationship (e.g., ``we hypothesize that \dots'', ``we predict \dots'', ``we expect \dots'', ``H1/H2 \dots'', or equivalent).

Strict requirements

     Use ONLY the provided Article Text. Do not invent or rewrite content.
     Your output must contain only complete sentences copied verbatim from the Article Text.
    
         You may select multiple sentences, and they may be non-consecutive.
         Do not output sentence fragments or stitched snippets.
         You may normalize whitespace/line breaks only.
    
     Prefer explicit hypothesis language (``hypothesize/predict/expect/propose'') or numbered hypotheses (H1/H2/H3).
     If multiple hypotheses are present, return the hypothesis (or small set of hypotheses) that most directly aligns with the Abstract Finding.
     Exclude results, statistical evidence, methods, and background framing unless the sentence explicitly states the hypothesis.


If no hypothesis statement is present in the provided text, output exactly: \textttNO TEXT.

Output format\\
Return only the extracted text in the following format (no extra commentary):